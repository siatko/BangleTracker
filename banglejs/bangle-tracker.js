/* jshint esversion: 6 */
/*
 * BangleTracker - WatchApp
 */

{
    // Screen
    const screen = {
        width:  g.getWidth(),
        height: g.getHeight(),
        top:    g.getHeight() / 3,
        center: g.getHeight() / 2,
        bottom: g.getHeight() / 1.25,
    }

    // Main colors
    const colors = {
        black:  '0x0000',
        white:  '0xffff',
        green:  '0x03e0',
        orange: '0xfd20',
        red:    '0x7800',
    }
}
